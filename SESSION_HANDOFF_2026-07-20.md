# Session Handoff — 2026-07-20

מסמך העברת-מקל לסשן העבודה של 20.7.2026. כל מה שלהלן **מוזג ל-`main`** (PRs #35–#40) ופרוס לפרודקשן ב-Vercel. המקור המחייב הוא `CLAUDE.md`; זה סיכום ממוקד של הסשן.

ענף העבודה: `claude/projects-archive-vpw75p`.

---

## 1. ארכיון פרויקטים ומסמכי סיום (PR #35)

**מה נבנה**
- **טבלת ארכיון** ב-`/projects/list`: פרויקטים בסטטוס `הסתיים`/`בוטל` יוצאים מהטבלה הראשית לטבלת ארכיון מתקפלת נפרדת — סינון לפי סטטוס, מיון לפי כל עמודה, חיפוש משותף, וסה"כ ערך. הסטטוס ניתן לעריכה גם בארכיון (שחזור לטבלה הראשית).
- **מסמכי סיום**: העלאת דוח סיום / תעודת אחריות כ-`attachments` (`file_type='completion_report'`/`'warranty_cert'`). ההעלאה מעדכנת `projects.last_updated_at` (תאריך הסגירה בארכיון) ומציעה לסמן `הסתיים`.
- טיפול בשגיאות שמירה ברשימה ובדף הפרויקט (עד עכשיו כשל ה-CHECK נבלע).
- פרויקטים בארכיון מוחרגים מ-KPIs בדשבורד ומדוח ההנהלה.

**באג שורש שתוקן** — האילוץ `projects_realization_status_check` הרשה רק 4 סטטוסים פעילים; סימון `הסתיים`/`בוטל` נדחה בשקט ע"י Postgres וה-UI "שיקר". 

**מיגרציה** `20260719_002` (הופעלה): הרחבת ה-CHECK ל-6 הערכים.

---

## 2. מודול רכש `/procurement` (PRs #36, #37)

**הרקע לבאג** — "צור הזמנת רכש" ביבוא יצר שורה בשקט בלי מסך; המשתמש לחץ 3 פעמים → 3 הזמנות כפולות. נוקו ידנית מול ה-DB (באישור): נמחקו PO-2026-002/003 + טיוטת אוטו כפולה (3 הזמנות + 84 שורות).

**מה נבנה — עמוד רכש (ניצן), הרשאת `import`**
- מקטעים: **ממתין להזמנת רכש** (הצעות חתומות ללא PO) · **הזמנות רכש בהכנה** (עורך מלא) · **הועברו לספק**.
- יצירת PO בלחיצה: מוגן מלחיצה כפולה, נרמול שם ספק (הסרת "(העתק)"), מילוי שם פרויקט, פתיחת העורך מיד. הפריטים נמשכים מ-**תמחור הספק** (`cost_input_items`) במחירי המקור — לא מהצעת הלקוח.
- **עורך PO**: ספק/מטבע/תאריך/מועד אספקה/Incoterms/תנאי תשלום/שם פרויקט/הערות רב-שורות + שורות (הוספה/עריכה/מחיקה, סה"כ אוטומטי).
- **שער ליבוא**: `import_orders.po_sent_at` — הזמנה עוברת ליבוא רק בלחיצת **"הועבר לספק"**. יצירה ידנית ביבוא ו-SmartUpload נחתמות כנשלחות; טיוטות אוטו מחתימה נוחתות ברכש.
- ביבוא: **"צפה בהזמנת רכש"** — מודל PDF קריאה בלבד (לנורית).
- דף פרויקט: רובריקת **"הזמנות רכש"** (`ProjectPOCard`) אחרי התמחור.

**מסמך `PODocument.tsx`** — A4 ממותג בסגנון `QuoteDocument`:
- **שפה לפי המטבע**: שקל → עברית RTL; מט"ח (USD/EUR/GBP) → **אנגלית מלאה LTR** (כל התוויות, תאריך "19 July 2026", יחידות יח׳→pcs/מטר→m/קומפלט→set).
- הערות ברירת מחדל (4 תנאים) — אנגלית למט"ח, עברית לשקל.
- **חלון תרגום AI** ("תרגם לאנגלית"): אוסף שם פרויקט/תנאי תשלום/הערות/תיאורי שורות בעברית → Gemini `mode:'text'` → מודל מקור↔אנגלית עם עריכה לפני "החל".
- חתימת פיברטק בלבד (חתימת ספק הוסרה).
- **עימוד מבוסס מדידה** (מראה נסתרת) — שורות לא נחתכות באמצע עמוד.
- **מספור PO** נגזר מההצעה: `HM-...` → `PO-...`. שם קובץ: `הזמנת רכש <פרויקט> <תאריך>`.

**בקרת אנומליה מול ההצעה המאושרת**
- השוואת שורות ה-PO ל-`quote_items` לפי זהות מפרט `DN|PN|SN` (צבירה). פערי כמות / מפרטים חסרים → פאנל אזהרה כתום חי; התאמה מלאה → שורה ירוקה.
- **"משוך כמויות מההצעה"** — יישור כמויות (התאמה חד-ערכית).
- **"תקן אנומליה אוטומטית"** — לפי הסדר: השלמת PN/SN חסרים בשורות שתואמות חד-משמעית → יישור כמויות → הוספת שורות חסרות (מחיר 0) → הסרת שורות אפס שאינן בהצעה. הדוח כולל **מספרי שורות**. הכל מקומי עד שמירה.

**מיגרציות** (הופעלו): `20260719_003` (`po_sent_at`/`po_sent_by`), `20260719_004` (`delivery_date`).

---

## 3. סידור מחדש של דף הפרויקט (PRs #38–#40)

- **"מפרט צינורות"** (טבלת DN/OD/ID) הוסר — "מפרטים" (קבצים) הוא המחליף.
- **"מעקב עדכונים ופגישות"** הועבר מיד מתחת ל"מידע בסיסי".
- **כרטיס "מסמכים" מאוחד** עם לשוניות (סגנון לשוניות התמחור) במקום 4 כרטיסים נפרדים:
  `מפרטים · שרטוטים · תכתובת · דוחות שירות שדה · תמונות · דוח סיום · תעודת אחריות`.
  גרירה מעלה ללשונית הפעילה; שרטוטים שומרים זיהוי מספר + עריכה; מפרטים/שרטוטים עדיין מוזנים לבוחר ההצעות ול-PDF. סוגים חדשים: `correspondence`/`field_report`/`photo`.
- **קבצי תמחור** — רק באזור התמחור. **אישורי הזמנה** — בלשונית "הזמנות" (`OrderDocs`, `entity_type='order'`).
- תוקנה שגיאת פתיחת קבצים → signed URL בטוח לספארי בכל הלשוניות.

**סדר הכרטיסים הסופי:** סטטוס → ציר עסקה → מידע בסיסי → מעקב עדכונים → תאריכים → אנשי קשר → סוג פרויקט → **מסמכים** → תמחור והצעות מחיר → הזמנות רכש → יבוא → סיפור הפרויקט.

---

## 4. `file_type`ים בטבלת `attachments` (מצב עדכני)

| file_type | entity_type | מוצג ב | בהצעה ללקוח? |
|---|---|---|---|
| `drawing` | project | לשונית שרטוטים · /drawings · בוחר הצעות | כן (לרוחב) |
| `spec` | project | לשונית מפרטים · בוחר הצעות | כן (לאורך) |
| `correspondence` / `field_report` / `photo` | project | לשונית ייעודית בכרטיס מסמכים | לא |
| `completion_report` / `warranty_cert` | project | לשונית ייעודית (מעדכן תאריך ארכיון) | לא |
| `supplier_quote` / cost files | cost_input | כרטיסי מקורות תמחור | לא (חסום לאנונימי) |
| `order_confirmation` | order | לשונית "הזמנות" בתמחור | לא |

---

## 5. מיגרציות הסשן (כולן הופעלו ב-DB)

- `20260719_002` — relaxed `projects.realization_status` CHECK (הסתיים/בוטל).
- `20260719_003` — `import_orders.po_sent_at` + `po_sent_by` (שער רכש→יבוא).
- `20260719_004` — `import_orders.delivery_date`.

**ללא שינויי RLS בכל הסשן.**

---

## 6. פתוח / לשיקול בהמשך

- עדכון אוטומטי של מספר ה-PO כשההצעה מקבלת גרסה חדשה (V07).
- טופס פריקה באתר (זמיר) — עדיין ממתין לטופס הנייר לדיגיטציה.
- מפרט הצינורות המובנה (`pipe_specs`) — הטבלה נשארה ב-DB ומזינה זיהוי PN/SN בהצעות, אבל אין יותר מסך עריכה ממנו בדף הפרויקט (הוסר לבקשת המשתמש).
- הרשאות: ודא שלניצן יש `import` edit ולנורית `import` view.
