# סקירת מערכת FibertechOS — 2026-07-02

> דוח סקירה מקיף של זרימת התהליכים במערכת: אוטומציה, שליטה ובקרה, קישוריות ונראות.
> הופק ע"י סקירה תלת-כיוונית (צד מכירות / צד תפעול / יכולות רוחב) על ענף `claude/fervent-mayer-dn0diw`.
> משמש כ-backlog עבודה — לעדכן סטטוס ✅ כשמשהו מטופל.

## 1. מפת הזרימה כפי שהיא היום

```
ליד (נוצר רק דרך רקסי) ──✗ אין המרה──▶ פרויקט (ידני)
                                          │
                                תמחור ספק (cost_input) ידני/רקסי
                                          │
                                הצעת מחיר (טיוטה) ידני
                                          │
                                "נשלח" = דגל בלבד (שיתוף בפועל ידני, out-of-band)
                                          │
                                נחתם (ידני + העלאת קובץ חתום)
                              ┌───────────┼──────────────┐
                              ▼ אוטומטי   ▼ אוטומטי       ▼ אוטומטי (2.7.26)
                       הזמנת ייצור   order_value      טיוטת הזמנת יבוא
                              │                            │
                       מתקדם רק ע"י                נורית: העלאה חכמה →
                       העלאת מסמכים                משלוח/מכולות/packing
                              │                            │
                              │                   "התקבל מול הוזמן" מחושב,
                              │                    אבל שינוי הסטטוס ידני
                              ▼                            ▼
                            ✗ אין שום קשר בין ייצור ליבוא
                                          │
                                          ▼
                     תעודות משלוח ללקוח + הנה"ח = ✗ מחוץ למערכת
                     מלאי = ✗ טבלה ריקה, שום דבר לא מזין/מוריד
```

**אוטומציות עסקיות קיימות (3):** חתימה→הזמנת ייצור · חתימה→order_value · חתימה→טיוטת יבוא.
כל שאר התהליך — ידני.

## 2. ממצאים קריטיים — שבור או מת

| # | ממצא | מיקום | סטטוס |
|---|---|---|---|
| 1 | התראת חתימה לכספים מתה — ה-webhook לא נקרא מעולם, וכותב לסכמה שלא קיימת (`profiles.role`, `alerts.severity`) | `app/api/webhooks/quote-signed/route.ts` | ✅ תוקן 2.7 — התראה in-app נכתבת מ-`/api/import/from-quote` |
| 2 | `valid_until` לא נכתב אף פעם → כל לוגיקת "פג תוקף" לא יכולה לפעול | `usePricing.ts` (createQuote/updateQuoteStatus) | ✅ תוקן 2.7 — נקבע sent_at+45 יום בעת שליחה |
| 3 | לוג הפעילות תמיד ריק — הכותב היחיד (`CommandBar`) הוא קוד מת; אין audit trail לכלום | `components/ai/CommandBar.tsx` | ✅ קוד מת נמחק 2.7; audit trail אמיתי — שלב ד' |
| 4 | 5/11 קישורי ניווט = 404 (`/marketing`, `/field`, `/inventory`, `/reports`, `/settings`); איסקור וטפסים לא בניווט בכלל | `Sidebar.tsx`, `BottomNav.tsx` | ✅ תוקן 2.7 |
| 5 | טפסי B116/B12-2/B165/B244 — כפתור שליחה עושה `console.log` בלבד; הנתונים נזרקים | `components/forms/Form*.tsx` | 🔲 פתוח — דורש טבלת `field_reports` + Storage |
| 6 | KPI "משלוחים בדרך" תמיד 0 (ניזון מהתראות שאיש לא יוצר); "נושאים פתוחים" מקודד 0 | `app/page.tsx:105,116-118` | ✅ תוקן 2.7 — נתונים אמיתיים מ-import_shipments/quotes/import_orders |
| 7 | איסקור רץ על סכמה לא קיימת — `v_iskoor_tracking` מפנה ל-`shipments`/`containers` שאין להן migration | `IskoorTracker.tsx`, `database/schema.sql:683` | 🔲 פתוח — להגר ל-`import_*` (שלב ד') |
| 8 | חיווי "מחובר" בצוות מזויף — נקודה ירוקה קבועה | `TeamStatus.tsx:73` | 🔲 פתוח (נמוך) |
| 9 | שני מנגנוני שלב לפרויקט מתפצלים — `projects.current_stage` קופא על 1, אבל הדשבורד סופר לפיו | `app/page.tsx:79-85` vs `StatusTracker` | 🔲 פתוח (שלב ד') |
| 10 | `AiSidebar` — קוד מת (לא מיובא בשום מקום) | `components/ai/AiSidebar.tsx` | ✅ נמחק 2.7 |

## 3. פערי-על (לפי צירים)

### 🤖 אוטומציה
- [ ] **מנוע התראות מתוזמן** (pg_cron / Vercel cron): הצעה שנשלחה ולא נענתה X ימים · valid_until מתקרב/עבר · ETA משלוח השבוע/עבר · דדליין ייצור · טיוטת יבוא ממתינה מעל X ימים. ⭐ המכפיל הגדול ביותר.
- [ ] **סטטוס נגזר ביבוא**: packing lines מכסים 100% → `received` אוטומטית; חלקי → `partially_received` (ידני נשאר כ-override).
- [ ] **המרת ליד→פרויקט** בלחיצה + טופס "ליד חדש" אנושי (היום לידים נוצרים רק דרך רקסי; `next_action`/`next_action_date` נכתבים ולא מוצגים).
- [ ] **קבלת סחורה מזינה מלאי** (כשמודול מלאי ייבנה): packing line התקבלה → `+`; תעודת לקוח → `−`.

### 🎛️ שליטה ובקרה
- [ ] **מסך שחרור תפ"י לניצן** — כפתור "✔️ שחרר" על טיוטת יבוא שכותב `reviewed_at`/`reviewed_by` (העמודות קיימות מ-`20260702_001`) ומעביר `draft→planned`. הבסיס למנוע ההצעות הלומד.
- [ ] **Audit trail כללי** (לא רק AI) — צד שרת: שינוי סטטוס הצעה, שחרור הזמנת יבוא, שינוי סטטוס ייצור, שינוי מחירים.
- [ ] **חתימה בצד שרת** — כל ה-cascade של החתימה (ייצור+order_value+יבוא) רץ היום בדפדפן החותם; route אחד `/api/quotes/sign` אטומי.
- [ ] **מקור אמת אחד לסטטוס הצעה** — 3 רשימות שונות היום (OpenQuotesWidget בלי 'signed', PricingSection בלי 'expired', כרטיס לקוח עם מצב חמישי).

### 🔗 קישוריות
- [ ] **ציר חיים של עסקה** — מסך אחד: הצעה→ייצור→PO→משלוח→קבלה→אספקה→חשבונית. להרחיב את 🗺️ מפת הקשרים הקיימת + להציג בעמוד הפרויקט.
- [ ] **חיבור ייצור↔יבוא** — צ'יפ "🚢 סטטוס יבוא + ETA" בדף ייצור, צ'יפ "🏭 סטטוס ייצור" בדף יבוא (שתיהן חולקות `quote_id`).
- [ ] **UI ל-`import_customer_deliveries`** — הטבלה קיימת עם דגלי נחתם/הועבר להנה"ח/הופקה חשבונית; אין שום מסך שכותב אליה. סוגר את הלולאה סחורה→נמסרה→חויבה.
- [ ] **חיבור טפסי שטח לפרויקט** — שמירה ל-DB+Storage עם `project_id` וצ'יפ בעמוד הפרויקט.

### 👁️ נראות
- [x] ~~תיקון ניווט~~ (2.7)
- [x] ~~KPI אמיתיים בדשבורד~~ (2.7)
- [ ] **חיווי "ההצעה נצפתה"** בווידג'ט הצעות פתוחות — `quote_views` נאסף אוטומטית אבל קבור בכרטיס ההצעה. זהב למכירות.
- [ ] **גיול הצעות** — "ימים מאז נשלחה" + צבע בווידג'ט.
- [ ] **פירוק עמוד הפרויקט** (1,561 שורות, 6 אזורי עריכה) לטאבים.
- [ ] **רווחיות אמיתית** בטבלת פרויקטים — הנקודה "מדמה רווחיות" לפי גודל עסקה; המרג'ין האמיתי קיים ב-`quote_items`.
- [x] ~~מעקב ספינה חי~~ (2.7) — ראה §4.

## 4. מעקב ספינות (נוסף 2.7.2026)

`GET /api/import/vessel-track?vessel=<name>` — צד שרת, דורש session.
- עם `DATALASTIC_API_KEY` ב-env: שולף מיקום חי, מהירות, יעד מדווח ו-ETA מ-Datalastic (vessel_find לפי שם → vessel_pro), ומחשב מרחק + ETA משוער לאשדוד וחיפה לפי המהירות הנוכחית.
- בלי key: מחזיר קישורים חיצוניים (VesselFinder / MarineTraffic חיפוש לפי שם) — הכפתור עדיין שימושי.
- UI: כפתור "🛰️ אתר ספינה" על כרטיס משלוח עם `vessel_name`.
- Cache בזיכרון 10 דק' לחיסכון בקרדיטים. ספק חלופי אפשרי: MyShipTracking / aisstream (websocket, פחות מתאים ל-serverless).

## 5. סדר עדיפויות

| שלב | תכולה | סטטוס |
|---|---|---|
| **א' — לתקן את השבור** | ניווט · התראת חתימה · valid_until · קוד מת · KPI | ✅ הושלם 2.7.2026 |
| **ב' — בקרה** | מסך שחרור תפ"י · מנוע התראות cron · סטטוס נגזר ביבוא · גיול הצעות + חיווי צפייה | 🔲 הבא בתור |
| **ג' — קישוריות** | ציר חיים · ייצור↔יבוא · UI תעודות לקוח/הנה"ח · טפסי שטח | 🔲 |
| **ד' — עומק** | חתימה בצד שרת + audit trail · פירוק עמוד פרויקט · איחוד סטטוסים · הגירת איסקור | 🔲 |
